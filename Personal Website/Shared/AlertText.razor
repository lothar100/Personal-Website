@if (string.IsNullOrEmpty(Error) == false)
{
    <div class="alert alert-danger p-1 m-0" role="alert" style="font-size: 10px; display:inline-block;">
        <strong>@Error</strong>
    </div>
}

@code {
    [Parameter]
    public string Error { get; set; }
}